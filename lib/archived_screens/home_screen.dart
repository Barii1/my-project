// This file previously contained an archived legacy HomeScreen implementation.
// The legacy content was removed permanently to keep the repository clean.
// Provide a harmless stub so accidental references won't fail analysis.

import 'package:flutter/widgets.dart';

class RemovedArchivedHomeScreen extends StatelessWidget {
  const RemovedArchivedHomeScreen({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
