// This file previously contained archived HomeScreenV2 content.
// The legacy implementation was permanently removed to avoid analyzer errors
// and reduce repository clutter. Keeping a tiny harmless stub so any accidental
// references that remain won't break static analysis.

import 'package:flutter/widgets.dart';

class RemovedArchivedHomeScreenV2 extends StatelessWidget {
  const RemovedArchivedHomeScreenV2({super.key});

  @override
  Widget build(BuildContext context) {
    // Intentionally empty.
    return const SizedBox.shrink();
  }
}
