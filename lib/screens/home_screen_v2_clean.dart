import 'package:flutter/material.dart';

// NOTE: This legacy file was archived to `lib/archived_screens/home_screen_v2_clean.dart` and
// replaced with an inert placeholder to remove it from active usage.
class RemovedHomeV2CleanPlaceholder extends StatelessWidget {
  const RemovedHomeV2CleanPlaceholder({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('This screen was removed and archived. See lib/archived_screens/home_screen_v2_clean.dart'),
      ),
    );
  }
}
